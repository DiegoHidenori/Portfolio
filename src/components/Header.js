import React from 'react';
import './Header.css';

function Navbar() {
    return (
        <nav>
            <div>
                <a href="#">Home</a>
                <a href="#">Projects</a>
                <a href="#">My Life</a>
                <a href="#">Goals</a>
            </div>
        </nav>
    );
}

export default Navbar;